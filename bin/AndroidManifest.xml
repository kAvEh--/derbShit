<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.eynakgroup.derbi"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="ir.eynakgroup.derbi.util.AppController"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomeActivity"
            android:label="NOTHING" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".onep.OnePlayerActivity"
            android:label="NOTHING" >
        </activity>
        <activity
            android:name=".onep.GameActivity"
            android:label="NOTHING" >
        </activity>
        <activity
            android:name="ir.eynakgroup.derbi.onep.OPLeaderBoardActivity"
            android:label="@string/title_activity_opleader_board" >
        </activity>
    </application>

</manifest>